// Hlíðarkaup Eldhús palette (from the chosen Color Hunt set)
export const C = {
  red: "#DB1A1A",     // primary / CTA (Hlíðarkaup tie-in)
  redDark: "#B81414",
  cream: "#FFF6F6",   // page background (warm white)
  teal: "#8CC7C4",    // soft accent
  tealSoft: "#E4F1F0", // tinted accent surfaces
  deep: "#2C687B",    // headings / deep accent
  ink: "#21323A",     // body text (near-black teal)
  muted: "#5C6B72",   // secondary text
} as const;
