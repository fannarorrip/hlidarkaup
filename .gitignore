.env.local
.env*.local

# Certificates / private keys — NEVER commit (búnaðarskilríki etc.)
*.pfx
*.p12
*.cer
*.pem
*.key
Lestu mig.txt

.next/
# Auto-generated by `next build`; the reference line flips between .next/types and
# .next/dev/types, which made `git pull --ff-only` fail on the build-on-server deploy.
next-env.d.ts
node_modules/
out/
orders.json
.DS_Store
.claude/
tsconfig.tsbuildinfo
ReglaWebService.2019.pdf
hlidarkaup-signature.pdf

# Local Netlify folder
.netlify
sjalfsali-applications.json
